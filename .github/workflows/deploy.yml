name: $bog_trip

permissions: write-all

on:
  workflow_dispatch:
  push:
    branches: [ main ]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:

  build:
    runs-on: ubuntu-latest

    steps:

    # Документ собран из деталей vmap, поэтому в бандл приложения приезжает и
    # node-тест пака деталей, а он требует собранного `bog/vmap/part/-/web.view.tree`.
    # В чистом CI его нет, и сборка приложения падает на чужом тесте. Поэтому
    # пак деталей собирается первым шагом, своим именем.
    - uses: hyoo-ru/mam_build@master2
      with:
        package: 'bog/vmap'
        modules: 'part'

    - uses: hyoo-ru/mam_build@master2
      with:
        package: 'bog/trip'

    - name: Cache-bust the bundle
      if: github.ref == 'refs/heads/main'
      run: |
        set -euo pipefail
        stamp="${GITHUB_SHA::7}"
        file="bog/trip/-/index.html"
        sed -i "s|src=\"web.js\"|src=\"web.js?v=$stamp\"|" "$file"
        grep -q "web.js?v=$stamp" "$file"

    - uses: hyoo-ru/gh-deploy@v4.4.1
      if: github.ref == 'refs/heads/main'
      with:
        folder: 'bog/trip/-'

    - name: URLs
      if: github.ref == 'refs/heads/main'
      run: echo "- https://b-on-g.github.io/trip/ — калькулятор поездки" >> "$GITHUB_STEP_SUMMARY"
